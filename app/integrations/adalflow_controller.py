"""AdalFlow integration placeholder.

Start with the local RuntimeController baseline. Migrate this module to wrap or replace
that logic when AdalFlow is added as an optional dependency.
"""


def status() -> dict[str, str]:
    return {"status": "not_implemented", "next_step": "map fallback graph to AdalFlow DAG"}
